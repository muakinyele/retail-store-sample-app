apiVersion: v1
kind: Service
metadata:
  name: retail-store-redis
  namespace: default
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: retail-store-redis
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: retail-store-redis
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: retail-store-redis
  template:
    metadata:
      labels:
        app: retail-store-redis
    spec:
      containers:
        - name: redis
          image: redis:7.0
          ports:
            - containerPort: 6379
